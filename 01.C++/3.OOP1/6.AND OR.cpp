#include <iostream>
#include <numeric>
#include <vector>

int main()
{
    int var1=0,var2=70;
    if(var1 and var2)
    {
        std::cout << "No zero value has been detected AND"<<std::endl;
    }
    if(var1 or var2)
    {
        std::cout << "No zero value has been detected OR"<<std::endl;
    }
    return 0;
}
